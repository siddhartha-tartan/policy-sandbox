import { atom } from "jotai";

export const dashboardSettingsAtom = atom<boolean>(false);
export const recentAttesstaionsAtom = atom<boolean>(false);
export const pendingQueriesAtom = atom<boolean>(false);
export const needsAttentionAtom = atom<boolean>(false);
export const recentAssessmentAtom = atom<boolean>(false);
